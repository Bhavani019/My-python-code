Input:
3,0
4,1.5708

Output:
5.0

-----------------------------------------------------------------------------

import math
r1,t1 = map(float,input().split(','))
r2,t2 = map(float,input().split(','))
x1 = r1*math.cos(t1)
y1 = r1*math.sin(t1)
x2 = r2*math.cos(t2)
y2 = r2*math.sin(t2)
total = math.sqrt(pow((x2-x1),2) + pow((y2-y1),2))
print(round(total,2))
